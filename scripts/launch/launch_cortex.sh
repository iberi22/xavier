#!/usr/bin/env bash
# ⚠️ DEPRECATED: Cortex has been removed from the SWAL stack (May 2026).
# This launcher was a script to run Xavier on port 8003 (old port, now 8006).
# Use `systemctl --user start xavier.service` or `xavier http` instead.
echo "CORTEX REMOVED — Xavier ahora corre en :8006."
echo "Usa: systemctl --user start xavier.service"
exit 1
