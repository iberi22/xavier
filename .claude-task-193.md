# Task: Memory Sync HTTP (#193)

You are a Rust engineer implementing the Memory Sync HTTP endpoints for Xavier.

## Context
- Repo: E:\cortex\xavier, branch: feat/memory-sync-http-193
- Existing sync module: src/memory/sync/ (merge.rs LWW, pull.rs, push.rs, mod.rs)
- Existing HTTP: src/adapters/inbound/http/ (routes.rs + handlers/)
- Project compiles and passes 135/135 integration tests

## Requirements

Implement these HTTP endpoints:

1. POST /api/v1/memory/sync/push — Push local changes to remote
2. POST /api/v1/memory/sync/pull — Pull changes from remote
3. GET /api/v1/memory/sync/status — Sync status
4. POST /api/v1/memory/sync/resolve/{conflict_id} — Resolve conflicts

## Instructions

1. Review src/memory/sync/ to understand the existing module
2. Review src/adapters/inbound/http/routes.rs for how routes are added
3. Create handlers in src/adapters/inbound/http/handlers/sync.rs
4. Register routes in routes.rs
5. Create integration tests for the new endpoints
6. Run 'cargo check' at the end to verify compilation
7. NEVER touch existing files outside sync and HTTP modules

Implement everything. Code must compile.
