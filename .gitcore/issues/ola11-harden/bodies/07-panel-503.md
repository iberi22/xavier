> Parent EPIC: {{EPIC}}

# [Ola 11 · 07] Panel missing-assets response: useful HTML stub (not bare 503 text)

> Wave **Ola 11 — Harden Residuals**. UX honesty for `/panel`.

## Current State (MEDIBLE)
- `src/server/panel/assets.rs` (~106 lines)
- Missing `index.html` → `503` plain text with build instructions
- API routes work without assets; shell does not

## Desired State (DELTA)
- When assets missing, return a minimal **HTML** page (still 503 or 200 with banner — prefer 503 + `text/html`) explaining how to build / set `XAVIER_PANEL_UI_DIR`
- Keep resolution order in `panel_ui_root()` intact
- Optional tiny unit/doc comment; no dependency on panel-ui source

## Web Research Required
1. search: `spa missing build assets fallback html 503 pattern 2025`
2. Read `panel_index` / `panel_ui_root` in assets.rs

## Exact Technical Context
- CRITICAL: ONLY `src/server/panel/assets.rs`
- Do not embed full React app
- Do not edit FEATURE_STATUS (issue 10)

## Problem
Operators hit opaque 503; need actionable HTML guidance in-browser.

## Acceptance Criteria
- [ ] Missing assets response Content-Type includes `text/html`
- [ ] Body mentions `pnpm run build` and/or `XAVIER_PANEL_UI_DIR`
- [ ] `cargo check -p xavier` 0 errors
- [ ] Only listed file changed

## Files to Modify
| File | Change | Risk |
|------|--------|------|
| `src/server/panel/assets.rs` | HTML stub on missing index | LOW |

## DO NOT touch
- `panel-ui/**`, `src/server/panel/mod.rs`, FEATURE_STATUS

## Verification
```bash
CARGO_TARGET_DIR=/tmp/rt cargo check -p xavier
```

## Dependencies & Merge Order
- **Expected effort:** Small
