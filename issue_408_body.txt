@google-labs-jules Please implement a stub for Fase 4 in a new module.

1. Create `src/memory/vram_sync.rs` and add `pub mod vram_sync;` to `src/memory/mod.rs`.
2. Inside `src/memory/vram_sync.rs`, define a background syncer trait/struct that will be responsible for reindexing.

```rust
use std::sync::atomic::{AtomicBool, Ordering};
use std::sync::Arc;

pub struct VramSyncer {
    pub is_syncing: Arc<AtomicBool>,
}

impl VramSyncer {
    pub fn new() -> Self {
        Self {
            is_syncing: Arc::new(AtomicBool::new(false)),
        }
    }

    pub fn trigger_sync(&self) {
        if !self.is_syncing.swap(true, Ordering::SeqCst) {
            // In a real implementation, this would spawn a tokio task.
            // For the stub, we just simulate starting and finishing immediately.
            self.is_syncing.store(false, Ordering::SeqCst);
        }
    }
}
```
3. Do NOT hook this into the server main loop yet. Just create the struct and logic stub.
