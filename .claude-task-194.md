# Task: libp2p Transport for Xavier Mesh (#194)

You are a Rust engineer implementing libp2p transport for Xavier mesh.

## Context
- Repo: E:\cortex\xavier, branch: feat/libp2p-transport-194
- Existing HTTP transport: src/mesh/transport.rs (MeshTransport)
- Existing protocol: src/mesh/protocol.rs (MeshHandshake, MeshManifest, etc)
- Existing ACL: src/mesh/acl.rs
- libp2p already in Cargo.toml as optional dep with feature "mesh"
- Project compiles, cargo check without --features mesh passes

## Requirements

Create a new transport module using libp2p that mirrors the existing HTTP transport interface:

1. Create src/mesh/libp2p_transport.rs:
   - Libp2pTransport struct (similar interface to MeshTransport)
   - Swarm setup with TCP+Noise+Yamux
   - mDNS for LAN peer discovery
   - Kademlia for WAN peer discovery
   - Identify protocol
   - PeerInfo from libp2p PeerId

2. Update src/mesh/mod.rs:
   - Add pub mod libp2p_transport behind #[cfg(feature = "mesh")]

3. All code behind #[cfg(feature = "mesh")] so cargo check --lib works

4. Run 'cargo check 2>&1 | Select-String -Last 5' at the end

IMPORTANT: Do NOT compile with --features mesh (libp2p takes too long). Just verify cargo check --lib passes.
