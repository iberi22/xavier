name = "xavier-panel"
compatibility_date = "2026-09-01"
pages_build_output_dir = "dist"

# Production & Subdomain bindings
# Target URLs: https://xavier.swal.network / xavier-panel.pages.dev
# Cloud Platform Gateway: https://xavier-cloud.swal.network
[vars]
ENVIRONMENT = "production"
DEFAULT_MEMORY_NAMESPACE = "swal/xavier/community"
XAVIER_CLOUD_ENDPOINT = "https://xavier-cloud.swal.network"

